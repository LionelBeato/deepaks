package deepaks;

public class Console {
	
	//TODO: initial prompt, I will have to make this the center feature as back end stuff wont be a priority at the moment!
	
	public String welcomeBlock() {
		String welcome = "Hello, there!";
		return welcome;
	}
	
	public void deptChoice(String input) {
		//sends to FrontDesk
		if (input == "Front Desk") {
			
		}
		//sends to Finance
		if (input == "Finance") {
			
		}
		//sends to Lease
		if (input == "Lease") {
			
		}
		//sends to Sales
		if (input == "Sales") {
			
		}
	}

}
